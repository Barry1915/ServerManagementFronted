# ServerManagementFronted
服务器管理系统前端

## 前端位置

前端项目在 `frontend/`，基于 **Vue 3 + Vite + TypeScript + Element Plus** 实现后台管理控制台。

## 快速启动

```bash
cd frontend
npm install
npm run dev
```

默认使用 Mock 数据（无需后端也能先看效果）。对接后端方式详见 `frontend/README.md`。
